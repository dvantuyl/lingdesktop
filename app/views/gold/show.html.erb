<div class='resource-show'>
<h2>
	<%= @resource.get_objects(RDF::RDFS.label => {:first=> true, :lang => @lang, :simple_value => :value}) %>
	<br/>
	<span style="font-size: 0.6em;">
		<a href='<%= @resource.uri %>' target="_blank"><%= @resource.uri %></a>
	</span>
</h2>

<hr/>

<strong class="label">Definition:</strong>

<% @resource.get_objects(RDF::RDFS.comment => {:lang => @lang, :simple_value => :value}).each do |comment| %>
	<p class="indent">
	  <%= "#{comment}" %>	
	</p>  
<% end %>

</div>