<div class='resource-show'>
<h2>
	<%= @lexicon.get_objects(RDF::RDFS.label => {:first=> true, :simple_value => :value, :context => @context}) %>
	<br/>
	<span style="font-size: 0.6em;">
		<a href='<%= @lexicon.uri %>' target="_blank"><%= @lexicon.uri %></a>
	</span>
</h2>

<hr/>

<strong class="label">Description:</strong>

<% @lexicon.get_objects(RDF::RDFS.comment => {:simple_value => :value, :context => @context}).each do |comment| %>
	<p class="indent">
	  <%= "#{comment}" %>	
	</p>  
<% end %>

</div>